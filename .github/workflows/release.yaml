name: "Release"

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to release into"
        type: environment
        required: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/workflows/actions/release.yaml
